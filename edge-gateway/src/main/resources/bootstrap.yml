management.security.enabled: false

spring:
  application:
    name: edge-gateway
  cloud:
    config:
      uri: http://localhost:8888
      
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888       